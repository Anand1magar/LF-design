import { AboutPage } from "@/components/pages/AboutPage";

export const metadata = {
  title: "About — LF Studio",
  description:
    "Meet the team behind LF Studio. We build the products that define the next.",
};

export default function About() {
  return <AboutPage />;
}
